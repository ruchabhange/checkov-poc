---
metadata:
  name: "Check if service account email is used"
  id: "CKV2_GCP_301"
  category: "GENERAL_SECURITY"

definition:
  resource_types:
    - google_compute_instance
  operator: exists
  attribute: service_account.email
  cond_type: attribute

  
